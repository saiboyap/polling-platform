apiVersion: v1
kind: Service
metadata:
  name: polling-frontend
  namespace: polling
  labels:
    app: polling-frontend
spec:
  selector:
    app: polling-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
